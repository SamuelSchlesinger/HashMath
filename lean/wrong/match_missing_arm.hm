-- Match with fewer arms than constructors (should be rejected)
import "../stdlib/Bool.hm"

def test : Bool → Prop :=
  fun (b : Bool) =>
    match b : Bool return Prop {
      | Bool.true => True
    }
